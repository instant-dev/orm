dist: jammy
language: node_js
node_js:
  - "20.6.1"
services:
  - postgresql
addons:
  postgresql:
    - "15.4"
before_script:
  - psql -c 'create database travis_ci_test;' -U travis
  - psql -c 'create database travis_ci_test_readonly;' -U travis
env: NODE_ENV=test
